{
    "properties": {
      "user_consent": {
        "type": "string",
        "minLength": 1
      },
      "ifsc": {
        "type": "string",
         "pattern": "^[A-Z]{4}[0][0-9]{6}$"
      },
      "account_number": {
        "type": "string",
        "minLength": 5,
        "maxLength": 25,
        "pattern": "^[a-zA-Z0-9]+$"
      }

    },
    "required": ["user_consent","ifsc","account_number"]
}